<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="colorPalette"
            type="com.sereem.remoteworker.ui.ColorPalette" />
    </data>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:layout_weight="5"
    >
    <ImageView
        android:id="@+id/userAvatar"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@{colorPalette.dark ? @drawable/ic_baseline_account_circle_white : @drawable/ic_profile_icon_circle}"/>

    <TextView
        android:id="@+id/parentHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginTop="10dp"
        android:textStyle="bold"
        android:textSize="20sp"
        android:text="parent"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:textColor="@{colorPalette.text}"
        />
</LinearLayout>
</layout>